{
    "name": "gmtk-2025",
    "scripts": {
        "dev": "vite --config vite/config.dev.mjs",
        "build": "vite build --config vite/config.prod.mjs",
        "deploy": "npm run build && git subtree push --prefix dist origin pages"
    },
    "devDependencies": {
        "typescript": "^5.8.3",
        "vite": "^6.3.5"
    },
    "dependencies": {
        "phaser": "^3.90.0",
        "terser": "^5.43.1"
    }
}